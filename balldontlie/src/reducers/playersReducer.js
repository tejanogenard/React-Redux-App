const initialState = {
    players: [],
    isFetching: false,
    error:""
}

export const playersReducer = (state = initialState, action) => {
    switch(action.typ){
        default: 
            return state;
    }
}